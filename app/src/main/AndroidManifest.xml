<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.besocial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon_"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon_"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeSocial">
        <activity android:name=".AddPostActivity" android:theme="@style/customizedStyle"/>
        <activity android:name=".ImagePickerActivity" android:theme="@style/customizedStyle"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>